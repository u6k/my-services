#!groovy

timestamps {
    try {
        node("master") {
            stage("crawle") {
                sh("docker exec ${env.APP_CONTAINER_NAME} rails crawl:stocks[${ARG_MISSING_ONLY}]")
                sh("docker exec ${env.APP_CONTAINER_NAME} rails crawl:stock_prices[${ARG_TICKER_SYMBOL},${ARG_YEAR},${ARG_MISSING_ONLY}]")
            }
        }
    } catch(Exception e) {
        slackSend(color: "danger", message: "Fail: ${env.JOB_NAME} #${env.BUILD_NUMBER} ${e}")
        throw e
    }
}
